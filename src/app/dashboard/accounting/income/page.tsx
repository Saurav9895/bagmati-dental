import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function IncomePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Income</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Income details will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
