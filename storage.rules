rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Grant read and write (create, update, delete) access to authenticated users
    // only on files within a specific patient's folder.
    match /patients/{patientId}/{fileId} {
      allow read, write: if request.auth != null;
    }
  }
}
