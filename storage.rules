rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Grant read, create, update, and delete access to any authenticated user,
    // but only for files within the 'patients' directory.
    match /patients/{patientId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
